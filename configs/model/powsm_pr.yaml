_target_: src.recipe.phone_recognition.model_module.PhoneRecognitionModel

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.001
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.LinearLR
  _partial_: true
  total_iters: 100
  last_epoch: -1

model:
  _target_: src.model.powsm.powsm_model.build_powsm
  work_dir: ${paths.exp_dir}/powsm_cache
  hf_repo: espnet/powsm
  # provide paths to override from local files instead of HF repo
  config_file: null
  model_file: null
  stats_file: null
  inference:
    _target_: src.model.powsm.powsm_inference.build_powsm_inference
    work_dir: ${paths.exp_dir}/powsm_cache
    hf_repo: espnet/powsm