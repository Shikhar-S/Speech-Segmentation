# @package _global_
# python src/main.py experiment=inference/segmentation_pxeus_bce inference.inference_runner.ckpt_path=<path>
defaults:
  - override /data: segmentation_train
  - override /data/tokenizer: xeuspr
  - override /logger: csv
  - override /model: null
  - override /model/net: powsm

task_name: "inf_seg_bce_pxeus"
tags: ["segmentation", "bce", "inference"]

train: false
test: false
distributed_predict: True

data:
  hf_repo: changelinglab/timit-segment

inference:
  num_workers: 4
  out_file: ${paths.output_dir}/seg_pxeus_bce.json
  passthrough_keys:
    - "utt_id"
    - "phone_timestamps"
    - "phones"
    - "split"
  inference_runner:
    _target_: src.recipe.segmentation.inference.build_boundary_inference
    ckpt_path: ???
    device: cuda
    threshold: 0.5
    net:
      _target_: src.model.xeusphoneme.builders.build_xeus_pr_from_hf
      work_dir: ${paths.cache_dir}/xeus
      hf_repo: espnet/xeus
      vocab_file: src/model/xeusphoneme/resources/ipa_vocab.json
      load_ckpt: false
      interctc_weight: 0.3
      interctc_layer_idx: [4, 8, 12]
      interctc_use_conditioning: true
      ctc_weight: 1.0
