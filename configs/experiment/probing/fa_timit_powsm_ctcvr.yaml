# @package _global_
# python src/main.py experiment=probing/fa_timit_powsm_ctcvr +trainer.precision=bf16
defaults:
  - override /data: timit
  - override /model: forced_alignment
  - override /model/net: powsm_ctc_vr
  - override /data/tokenizer: powsm  # FINE FOR NOW BECAUSE TOKEN LIST IS SAME!
  - override /trainer: gpu
  - override /callbacks: default
  - override /logger: wandb

task_name: "fa_timit_powsm_ctc_vr"
tags: ["timit", "powsm_ctc_vr", "fa"]

seed: 42

trainer:
  min_epochs: 10
  max_epochs: 10
  gradient_clip_val: 1.0
  val_check_interval: 0.25 # validate 4 times per epoch
  callbacks:
    - learning_rate_monitor

model:
  optimizer:
    lr: 0.0002

data:
  batch_size: 32

logger:
  wandb:
    tags: ${tags}
    group: ${task_name}
  aim:
    experiment: ${task_name}  