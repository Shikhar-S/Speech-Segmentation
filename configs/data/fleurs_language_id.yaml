_target_: src.data.fleurs.language_id.FleursLanguageId
target_sr: 16000
max_audio_length: 20.0
config_name: "all"  # Use "all" to load all languages for language identification task
batch_size: 48 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 10
pin_memory: False

