_target_: src.data.timit.common_datamodule.TimitDataModule
timit_root: ${paths.download_dir}/TIMIT/timit_nltk
local_cache_path: ${paths.cache_dir}/timit
tokenizer: ??? # defined in experiment config
batch_size: 48 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 10
pin_memory: False
mask_probability: 0.0
target_sr: 16000