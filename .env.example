# Copy to .env (untracked). Hydra reads these via ${oc.env:...}; run scripts
# via the shell environment (sbatch forwards it). See README "Environment variables".
PROJECT_ROOT="."
HF_HOME="exp/cache/hf"
# HF_TOKEN=hf_...                 # private changelinglab/* datasets
# PRISM_DUMP_DIR=exp/data/prism   # Kaldi dumps for Table II recognition sets
# XEUSPR_CKPT=/path/to/checkpoint-22000.ckpt   # PhoneticXEUS topline
# MFA_ROOT_DIR=exp/cache/mfa
# MAMBA_ROOT_PREFIX=~/micromamba
